---
name: jg-article-humanizer
description: 对已有中文文章初稿进行「镜哥文章拟人化改写」，恢复真实工作场景、口述思路、段落呼吸和个人判断，并在锁定事实与关键排版的前提下加入恰好 3 处随机文字毛边。仅在用户要求拟人化二改、真人化改写、降低 AI 味或明确点名本 Skill 时使用；不用于从零起稿、事实扩写或普通校对。
---

# 镜哥文章拟人化改写

这是初稿完成后的第二遍编辑。核心不是给文章撒一层口语词，而是恢复一个真人可能经历的思考过程：为什么关注、拿什么验证、看到什么、怎么判断，以及这件事与真实工作有什么关系。

## 改写边界

- 不新增未经素材支持的经历、人物关系、实测结果、数据或立场。
- 不改变事实、观点方向和章节顺序。
- 专名、型号、数字、时间、价格、引语、链接、功能名称和用户确认的品牌文案是禁改区。
- 用户只标注部分内容时，只改标注范围；上下文仅用于理解。
- 用户在本轮对话中明确纠正过的表达，形成「当篇作者覆盖规则」，优先级高于本 Skill 的通用偏好。不得在后续版本中回退。

开始前必须完整读取 [references/author-fingerprint.md](references/author-fingerprint.md)。

- 输入是 1200 字以上公众号、博客或已配图长文时，再读取 [references/longform-workflow.md](references/longform-workflow.md)。
- 遇到抽象判断、产品评论或收尾段时，读取 [references/association-editorial-guide.md](references/association-editorial-guide.md)。
- 输入包含 Markdown、HTML、代码块或小绿书排版时，读取 [references/format-preservation.md](references/format-preservation.md)。
- 交付前必须读取 [references/human-edge-policy.md](references/human-edge-policy.md)，执行恰好 3 处随机文字毛边。

## 工作流程

### 1. 建立四张内部清单

改写前先在内部提取，不向用户展示：

1. **事实锁**：专名、数字、时间、功能、结论边界和已确认文案；
2. **作者覆盖规则**：用户本轮明确要求保留、删除、替换或避免的表达；
3. **现场锚点**：真实时间、身份、项目背景、操作、结果和限制；
4. **结构锁**：标题、媒体、链接、列表、重点样式和不可移动节点。

用户最新手改稿是最高优先级样本。对照改前与手改稿，提炼稳定动作，不要只记住某个句子。

### 2. 先改思路，再改句子

优先处理这些 AI 痕迹：

- 标题后立刻出现报告式结论；
- 背景、动作、结果和判断挤在一个长段；
- 案例在最后一张图后突然结束；
- 全文都按「事实—解释—总结」匀速推进；
- 为制造对立而反复使用「不是……而是……」「没有……而是……」；
- 使用「跑了、跑通、拉通、闭环、承接」等作者未认可的行业黑话。

优先恢复「现场背景 → 实际动作 → 可见结果 → 个人判断」的自然路径。允许一句关键判断独立成段，也允许先说感受，再补原因。

### 3. 加入作者存在感

作者存在感优先来自：

- 与内容有关的工作背景；
- 「我为什么会想到测这个」的临场动机；
- 对结果的明确判断和边界说明；
- 读者在相同场景中会碰到的具体麻烦；
- 必要的插话、追问、括号旁白或联想。

「说实话」「你看」「大家可以先看一下」「这里就不得不提一下」只有在承担过渡或判断功能时使用。用户口述中出现过的表达可以保留，模型不得把它们当密度指标。作者自称可偶尔使用「镜哥」，但连续章节不要用同一种方式刷存在感。

联想和比喻是可选手段，不是拟人化的核心。长文每个主要章节至多选择 1—2 处真正能解释机制、差距或代价的联想；没有合适位置时不强加。

### 4. 调整段落呼吸

- 开场判断、章节转向和关键结论可以单独成段；
- 一个自然段通常只承担一个主要动作；
- 长短段落交替，避免连续三段结构完全一致；
- 标题后的首段先完成过渡，再进入总结；
- 案例背景与具体操作通常分开；
- 案例最后一张证据图之后，补一段可复用判断、现实价值或边界，除非原文已经完成收束。

### 5. 加入恰好 3 处随机毛边

每篇完成拟人化的文章必须出现 **恰好 3 处** 有意文字毛边。随机规则、候选类型和禁区见 [references/human-edge-policy.md](references/human-edge-policy.md)。

三处毛边必须：

- 分布在至少两个普通叙述段落中；
- 混合错别字、同音混用或连续重复字，不得三处完全同型；
- 不影响事实理解，不落在标题、专名、数字、链接、引语、图注、CTA 或关键结论中；
- 在内部建立三项清单并复核，最终不向读者解释或标记。

这条规则只受用户当次明确要求覆盖；用户要求零错字、正式公文或标准校对时，以当次要求为准。

## 格式模式

- `flow`：小绿书、朋友圈、即刻和普通短图文。保护平台外壳，允许拆段、并段和插入独立短句。
- `longform`：公众号、博客、1200 字以上 Markdown 配图长文。锁定标题层级、图片顺序、链接、列表、重点样式和章节顺序，允许为了真人节奏拆段、并段和增加章节内过渡段。
- `strict`：HTML、代码密集内容、局部改写或用户明确要求结构不动的稿件。只改允许范围内的文本节点或自然语言，不移动结构节点。

自动模式优先按内容选择：小绿书外层代码块使用 `flow`；1200 字以上 Markdown 长文使用 `longform`；HTML 和其他内容使用 `strict`。详细规则见 [references/format-preservation.md](references/format-preservation.md)。

## 交付前复核

- 是否保持事实、观点、章节顺序和所有禁改内容；
- 是否执行了用户本轮最新修改，没有旧表达回退；
- 标题后的第一段是否自然过渡；
- 是否出现真实场景、实际动作、结果和个人判断，而非只换口头禅；
- 段落是否有长短变化，案例是否完整收束；
- 联想是否真的帮助理解，没有机械堆梗；
- 是否恰好有 3 处随机毛边，且均位于安全位置；
- 图片、链接、标题、重点样式和品牌文案是否完整保留；
- 专名是否统一，用户提供的 Slogan、网址和最终句子是否逐字保持。

格式复核可运行：

```bash
python3 scripts/verify_format_preservation.py original.md rewritten.md --mode auto
```

语言复核可运行：

```bash
python3 scripts/lint_humanized_article.py rewritten.md
```

脚本只能提示高风险项，无法替代对事实、语义和三处毛边的人工复核。

最终只交付保持目标格式的改写稿。用户要求分析或对照时，才额外说明改动。
